<!DOCTYPE html>
<html class="antialiased">
<%= render "shared/head" %>

<body class="font-sans font-normal leading-normal text-gray-800 bg-white flex flex-col h-screen">
<%= render "shared/flash_notice" %>
<%= render "shared/header" %>

<main class="px-4 lg:px-10 flex-1">
  <%= content_for?(:content) ? yield(:content) : yield %>
</main>
</body>

<%= debug params if Rails.env.development? %>
<%= render 'shared/footer' %>

</html>
